## 1. Sidebar History Layout

- [x] 1.1 将 [App.tsx](d:\company\FileFlash-Bridge\App.tsx) 中的项目历史整理为主工作台左侧边栏，并保留现有服务状态与主内容布局。
- [x] 1.2 引入 `react-native-paper`，在 App 根部接入 `PaperProvider`，并使用 Paper Drawer 体系承载项目历史与活跃项目高亮态。
- [x] 1.3 为窄屏补齐真正的左侧 Drawer 抽屉、状态栏安全区域内展开、关闭遮罩与空态，确保小屏下不会把项目历史直接堆到主工作台内容里。
- [x] 1.4 将项目历史内容改为列表行样式，展示项目名称、创建日期、文件数/消息数等摘要，并将窄屏外部入口收敛为菜单图标按钮。

## 2. Project Actions

- [x] 2.1 将侧边栏中的新建、切换和项目行 `...` 菜单删除接入现有 `createProject`、`selectProject`、`deleteProject`，并保持 `busyAction` 禁用与删除确认提示不变。
- [x] 2.2 验证并修正活跃项目切换后的联动显示，确保顶部摘要、消息列表、文件列表和共享文件跳转都指向新的活跃项目。
- [x] 2.3 清理覆盖层/回顶方向的残留实现，确保项目历史需求完全收敛到“侧边栏 + Paper Drawer”方案。
- [x] 2.4 将“启动服务”和“选文件”移回主页工作区，确保项目历史侧边栏只承担历史浏览与项目管理职责。
- [x] 2.5 移除侧边栏卡片式包装、内部关闭按钮和全局“删除当前项目”按钮，并将“新建”收敛到标题栏右上角。

## 3. Verification

- [x] 3.1 补充共享代码验证或交互测试，覆盖 Drawer 渲染、项目切换、项目行 `...` 删除菜单和删除确认行为。
- [ ] 3.2 在 iOS 真机上验收侧边栏布局、Drawer 项目切换、标题栏新建与项目行 `...` 删除的交互。
- [ ] 3.3 在 Android 真机上验收侧边栏布局、Drawer 项目切换、标题栏新建与项目行 `...` 删除的交互。
- [ ] 3.4 执行局域网回归验证，确认切换项目后浏览器提交的文本和文件进入新的活跃项目，且共享文件下载、分块重试、现有本地网络与文件权限流程没有回归。

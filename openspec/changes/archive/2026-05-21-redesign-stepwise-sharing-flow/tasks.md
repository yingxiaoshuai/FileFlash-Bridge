## 1. 首页流程重组

- [x] 1.1 在 `HomeScreen` 中派生服务流程阶段，区分未启动、启动中、运行可达、错误/不可达状态，并用测试数据确认每个阶段可独立渲染。
- [x] 1.2 将未启动状态的服务主操作改为居中大圆形启动入口，点击后复用现有 `model.toggleService()` 启动逻辑，启动中禁止重复触发。
- [x] 1.3 在未启动状态折叠完整共享文件列表和项目内容列表，仅保留真实可操作状态、共享/项目摘要或启动后可继续管理的短提示，不把首页做成使用说明页。
- [x] 1.4 在运行且地址可达状态展开访问地址、二维码、复制链接、刷新地址、停止服务、共享文件区和当前项目内容区，并确认既有数据不被重置。

## 2. 视觉与交互 polish

- [x] 2.1 在 `appShellStyles` 中新增或调整大圆形启动入口、步骤容器、辅助状态区和运行后展开区域样式，确保使用现有 theme token。
- [x] 2.2 优化窄屏、横屏和宽屏布局，确保大圆形入口、网络标签、安全模式、错误提示和按钮文本不重叠、不溢出。
- [x] 2.3 调整运行后服务区视觉层级，让地址/二维码与共享文件和项目内容按“启动后继续操作”的顺序呈现。
- [x] 2.4 更新中文与英文 i18n 文案，覆盖第一步启动、启动中、启动失败重试、启动后共享文件和项目内容提示，并删除会让首页像教程或使用说明的大段说明文案。
- [x] 2.5 调整语言切换入口文案：中文界面默认显示 `English`，英文界面默认显示 `中文`，并保持菜单或设置项中的当前语言状态可理解。

## 3. 引导与状态反馈

- [x] 3.1 调整工作台新手引导文案，使未启动状态只辅助解释中心启动入口，运行状态再解释地址、共享文件和项目内容，避免把首页主体变成引导说明。
- [x] 3.2 调整引导锚点测量逻辑，确保折叠区域不可见时不会高亮错误位置或遮罩不存在的目标。
- [x] 3.3 将权限缺失、网络不可达、端口绑定失败等启动失败原因展示在启动步骤内，并保留可重试入口。
- [x] 3.4 确认停止服务后首页回到启动步骤，且不会展示误导性的可复制地址或二维码。

## 4. 传输与权限回归

- [x] 4.1 验证浏览器上传文件和文件夹仍写入当前会话 App 内部存储，不会因 UI 改版自动保存到系统公共目录。
- [x] 4.2 验证浏览器提交文本仍追加到当前活跃项目，不覆盖既有消息，不改变项目归属。
- [x] 4.3 验证浏览器下载手机共享文件、App 内单文件导出和批量下载/保存仍复用现有显式保存流程。
- [x] 4.4 验证安全模式开关、复制链接、刷新地址、二维码和停止服务在步骤式 UI 中保持原有语义。

## 5. 自动化与双端验收

- [x] 5.1 补充或更新首页渲染测试，覆盖未启动大圆形入口、启动中禁用态、运行后展开共享文件和项目内容。
- [x] 5.2 补充或更新语言入口测试，覆盖中文界面显示 `English`、英文界面显示 `中文` 和点击后的切换行为。
- [x] 5.3 执行 `npm run typecheck`，修复新增类型或 JSX 结构问题。
- [x] 5.4 执行 `npm run lint` 或记录当前无法通过的既有 lint 问题，确保本次变更不新增明显违规。
- [x] 5.5 执行可用的 Jest 测试，至少覆盖工作台模型、首页渲染、语言切换或传输相关回归。
- [ ] 5.6 在 Android 真机或稳定模拟器验收：启动服务、局域网浏览器访问、文件上传、文本提交、共享文件下载、导出保存、语言切换、停止服务和错误重试。
- [ ] 5.7 在 iOS 真机或模拟器验收：启动服务、权限提示、局域网浏览器访问、文件上传、文本提交、共享文件下载、系统保存/分享确认、语言切换和停止服务。
- [ ] 5.8 如本地可用，回归 Harmony 构建或真机：确认步骤式 UI 不影响现有本地服务启动、浏览器访问和共享下载路径。

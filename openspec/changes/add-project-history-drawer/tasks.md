## 1. Workspace History Entry

- [ ] 1.1 将 [App.tsx](d:\company\FileFlash-Bridge\App.tsx) 中常驻左侧“项目历史”区域改为主工作台左上角入口，并调整顶部栏布局以容纳历史入口与服务状态信息。
- [ ] 1.2 实现响应式历史项目管理层，在宽屏下提供抽屉式面板、在窄屏下提供覆盖式面板，并复用现有项目卡片摘要展示当前活跃项目高亮态。
- [ ] 1.3 为历史项目管理层补齐空态、关闭入口和交互屏蔽，确保打开后不会破坏主工作台现有滚动和按钮点击行为。

## 2. Project Actions and Home Return

- [ ] 2.1 将历史项目管理层中的新建、切换、删除操作接入现有 `createProject`、`selectProject`、`deleteProject`，并保持 `busyAction` 禁用与删除确认提示不变。
- [ ] 2.2 为主工作台根 `ScrollView` 增加回顶控制，让“新建项目”和“切换历史项目”成功后自动关闭历史层并回到默认总览态。
- [ ] 2.3 验证并修正活跃项目切换后的联动显示，确保顶部摘要、消息列表、文件列表和共享文件跳转都指向新的活跃项目。

## 3. Verification

- [ ] 3.1 补充共享代码验证或交互测试，覆盖历史层展开/收起、项目切换、删除后活跃项目回退和回到主页总览态的行为。
- [ ] 3.2 在 iOS 真机上验收左上角历史入口、历史层布局、新建/切换/删除项目，以及切换后回到主页总览态的交互。
- [ ] 3.3 在 Android 真机上验收左上角历史入口、历史层布局、新建/切换/删除项目，以及切换后回到主页总览态的交互。
- [ ] 3.4 执行局域网回归验证，确认切换项目后浏览器提交的文本和文件进入新的活跃项目，且共享文件下载、分块重试、现有本地网络与文件权限流程没有回归。

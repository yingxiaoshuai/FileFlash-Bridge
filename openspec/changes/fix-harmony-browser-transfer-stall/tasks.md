## 1. 传输链路确认与边界常量

- [ ] 1.1 复现鸿蒙门户上传/下载卡住问题，记录触发文件大小、浏览器类型、接口路径和 App 侧 `Property storage exceeds 196607 properties` 日志。
- [x] 1.2 梳理 `portalDocument.ts`、传输服务控制器、React Native HTTP runtime、Harmony 静态服务 TurboModule、文件访问桥中的二进制传递边界。
- [x] 1.3 定义共享的传输分块配置，区分浏览器逻辑 chunk、下载并发数、Harmony JS/Native 安全桥接 chunk，并确保配置不使用 base64。

## 2. Harmony 本地服务与文件桥修复

- [x] 2.1 修改 Harmony 静态服务请求体处理，避免把超过安全上限的上传 body 作为单个 `Uint8Array` 传入 RN JS。
- [x] 2.2 修改上传保存路径，使大文件和 JSON 文件都按字节小块追加写入 App 内部会话存储，并保留文件名、相对路径和原始内容。
- [x] 2.3 修改共享下载响应路径，使大文件读取和 socket 写出按安全块流转，不在 RN JS 内构造完整文件字节数组。
- [x] 2.4 捕获 Harmony 文件桥、静态服务和 Hermes/RNOH 边界异常，返回结构化 HTTP 失败状态并防止半写入文件被标记成功。

## 3. 浏览器门户上传修复

- [x] 3.1 调整门户上传分片实现，优先直接发送 Blob/ArrayBuffer 分片，避免为过大分片创建会被枚举展开的超大 `Uint8Array` 对象。
- [x] 3.2 为上传请求补齐超时、取消、失败状态清理和可重试 UI，避免按钮无响应或长期停在 0%。
- [ ] 3.3 验证普通文件、JSON 文件、压缩包和大文件上传时进度能推进，失败时能显示明确原因。

## 4. 浏览器门户下载修复

- [x] 4.1 重构共享文件下载流程，移除完整 `chunkPlan`、`downloadedChunks` 和无界进度数组对大文件的依赖。
- [x] 4.2 实现有界内存下载路径：支持顺序或低并发拉取分块，并在可用时增量写入浏览器文件保存能力；小文件保留受控 Blob 降级。
- [x] 4.3 为下载请求补齐超时、取消、重试耗尽提示和按钮状态恢复，避免下载停在“下载中 0%”。
- [x] 4.4 为浏览器共享下载区增加批量选择、全选、清空、已选数量和下载选中入口，批量操作复用单文件下载状态和分块策略。
- [ ] 4.5 验证多个共享文件连续下载、批量下载、单个分块失败、服务停止和浏览器刷新后的状态恢复。

## 5. App 端共享下载文案与批量选择

- [x] 5.1 调整 App 共享文件区文案，明确“添加到共享”“选择下载”“下载选中”“已选数量”等动作，避免把共享、下载和导出混淆。
- [x] 5.2 为 App 共享文件区增加选择下载模式，支持选择/取消单个共享文件、全选、清空和退出选择模式。
- [x] 5.3 为 App 共享文件区增加下载选中操作，批量复用现有显式导出/保存流程，并处理空选择、单项失败和整体完成提示。
- [ ] 5.4 验证 App 端单文件下载/保存、批量下载/保存、取消系统保存器和失败提示在 Harmony、Android、iOS 上的表现。

## 6. 跨平台回归与真机验收

- [ ] 6.1 在 Harmony 真机或稳定模拟环境验证：Android/桌面浏览器上传 JSON 文件不返回 500。
- [ ] 6.2 在 Harmony 真机或稳定模拟环境验证：上传大文件不会触发 Hermes property storage 报错，App 不会卡死。
- [ ] 6.3 在 Harmony 真机或稳定模拟环境验证：浏览器下载共享大文件进度推进并能保存到浏览器侧。
- [ ] 6.4 在 Harmony 真机或稳定模拟环境验证：浏览器批量选择下载多个共享文件时不会卡在 0%，失败项可见。
- [ ] 6.5 在 Android 与 iOS 现有构建上回归门户上传、文本提交、共享列表、单文件下载和批量下载，确认非鸿蒙平台无退化。
- [x] 6.6 执行可用的 TypeScript、lint 或单元测试命令，并记录无法自动化覆盖的真机验证项。

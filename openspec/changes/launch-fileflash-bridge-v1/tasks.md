## 1. Project Foundation

- [ ] 1.1 初始化 React Native + TypeScript 项目结构，并创建 `service`、`file-access`、`portal`、`security` 共享模块边界
- [ ] 1.2 引入并配置本地 HTTP 服务、文件访问、二维码和网络状态所需依赖，记录 iOS/Android 安装与构建要求
- [ ] 1.3 定义统一的服务状态模型、配置类型和错误码，供 App UI、服务层和浏览器门户共享

## 2. Local Transfer Service

- [ ] 2.1 实现本地传输服务控制器，支持显式启动、停止、地址刷新和运行状态同步
- [ ] 2.2 实现网络模式探测与地址解析，覆盖公共 Wi-Fi 与手机热点场景
- [ ] 2.3 实现入站文件上传、目录结构接收、文本提交和状态查询接口
- [ ] 2.4 实现端口占用、服务中断、无可用网络等异常状态提示与恢复入口

## 3. File Access and Platform Adapters

- [ ] 3.1 实现共享的 Inbound Storage Gateway 接口，统一目标目录授权、写入流、文件名冲突处理和临时文件清理
- [ ] 3.2 完成 iOS 目录授权、本地网络权限和文件访问适配，并验证授权失败后的恢复流程
- [ ] 3.3 完成 Android Storage Access Framework、网络权限和内容 URI 访问适配，并验证多目录/大文件场景

## 4. Browser Transfer Portal

- [ ] 4.1 实现由 App 本地服务托管的响应式 Web 门户页面和同源 API 接口
- [ ] 4.2 实现浏览器端文件选择、拖拽上传、目录上传兼容处理和文本粘贴提交交互
- [ ] 4.3 实现浏览器端上传进度、文本提交反馈、成功提示和失败重试提示
- [ ] 4.4 完成桌面浏览器与移动浏览器的双端 UI 验收，确保窄屏与宽屏都可完成核心流程

## 5. Security and Session Controls

- [ ] 5.1 实现简单模式 / 安全模式切换，保护门户访问、上传接口和文本提交接口
- [ ] 5.2 实现活动连接数统计与最大会话数限制，并在 App 界面展示当前活跃连接状态
- [ ] 5.3 实现“简单模式 / 安全模式”切换：简单模式仅 URL；安全模式 URL 携带 `key`，缺失/错误直接拒绝访问
- [ ] 5.4 实现 `key` 生成、一键刷新与失效策略（刷新后旧 key 立即失效），并确保二维码与复制链接同步更新
- [ ] 5.5 实现服务手动停止后的连接清理与浏览器断开提示

## 6. Incoming Content Handling

- [ ] 6.1 实现手机端文本接收区，展示最近一次浏览器提交的文本内容、来源时间和接收状态
- [ ] 6.2 实现文本内容的一键复制、清空与覆盖策略，避免误覆盖用户已有剪贴板
- [ ] 6.3 实现入站文件保存结果反馈、目标目录提示和失败原因展示
- [ ] 6.4 真机验证：浏览器上传文件、上传文件夹或提交文本内容后，手机端能正确接收并反馈结果

## 7. Background Keep-Alive (App Background / Screen Off)

- [ ] 7.1 Android：实现传输服务前台服务化（Foreground Service），包含常驻通知、服务启停联动与权限/兼容性验证
- [ ] 7.2 iOS：实现后台执行与服务保活策略（原生后台任务窗口 + 状态同步），覆盖锁屏/切后台的可访问性验证
- [ ] 7.3 实现后台/前台切换时的状态一致性：URL/二维码/连接数不丢失，必要时自动恢复服务并提示

## 8. Verification and Release Readiness

- [ ] 8.1 在 iOS 真机上验证公共 Wi-Fi、热点、权限拒绝、服务重启和“浏览器 -> 手机”主路径
- [ ] 8.2 在 Android 真机上验证公共 Wi-Fi、热点、权限拒绝、服务重启和“浏览器 -> 手机”主路径
- [ ] 8.3 执行大文件上传、目录上传和文本提交失败重试验证，并记录边界条件
- [ ] 8.4 完成安全验证，确认简单/安全模式、key 校验、连接限制与服务停止行为符合规格
- [ ] 8.5 完成后台保活验证：锁屏/切后台后浏览器仍可继续向手机提交内容，停止服务后浏览器侧提示断开

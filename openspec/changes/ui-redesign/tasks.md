## 1. 文档与基线确认

- [x] 1.1 确认 `ui-redesign` 文档不再要求安装 `react-native-sf-symbols`、BlurView 或 Reanimated 作为 V1 必需依赖
- [x] 1.2 记录当前 `package.json` UI 依赖基线，确认本次 V1 不新增原生 UI 依赖
- [x] 1.3 运行 `npm run typecheck` 建立改造前类型基线
- [x] 1.4 运行 `npm run test:unit -- --runInBand` 建立改造前单元测试基线

## 2. 设计 token 与回滚基础

- [x] 2.1 创建 `src/app/colors.ts`，定义跨平台语义色彩
- [x] 2.2 创建 `src/app/tokens.ts`，定义 8pt 间距、圆角、阴影、图标尺寸和动效参数
- [x] 2.3 更新 `src/app/theme.ts`，集成新的 color/token，同时保留现有字段兼容
- [x] 2.4 更新 `src/app/paperTheme.ts`，让 React Native Paper 使用新 token
- [x] 2.5 备份旧主题或添加轻量 feature flag，便于回滚 UI 改造

## 3. 项目本地图标体系

- [x] 3.1 扩展 `src/app/icons/AppIcons.tsx`，补充菜单、帮助、添加、关闭、复制、刷新、分享、下载、更多、删除等工具图标
- [x] 3.2 确认 Android/Harmony 的 `AppIcons.android.tsx` 与 `AppIcons.harmony.tsx` 有可用降级
- [x] 3.3 将 `GlyphIconButton` 从任意文字符号输入调整为项目图标名称或图标组件输入
- [x] 3.4 替换首页、设置页、底部 Tab 和引导入口中的高频文字符号按钮
- [x] 3.5 为所有图标按钮保留 `accessibilityLabel`、稳定 `testID` 和至少 44px 触控面积

## 4. 共享 UI 组件升级

- [x] 4.1 升级 `ActionButton`，使用新 token 的主按钮、次按钮、危险按钮和禁用态
- [x] 4.2 升级 `PanelSurface`，使用统一表面、边框、高光线和跨平台阴影
- [x] 4.3 升级 `FeedbackBanner`，统一 info/success/error 的状态色和操作区
- [x] 4.4 升级 `EmptyStateCard`，避免大块空白或廉价虚线堆叠
- [x] 4.5 检查共享组件不得出现卡片套卡片的重复装饰

## 5. 首页分步工作流

- [x] 5.1 更新 `HomeScreen`，当服务未运行或地址不可达时，将启动服务作为首屏中心主操作
- [x] 5.2 启动中状态禁用重复触发，并展示等待权限、网络或服务结果的反馈
- [x] 5.3 服务未启动时，将共享文件和项目内容压缩为摘要、预览或隐藏，不抢主焦点
- [x] 5.4 服务运行且地址可达后，将服务控制压缩为在线摘要
- [x] 5.5 服务运行后突出“内容与共享工作区”，并保留二维码、复制链接、刷新地址和停止服务入口
- [x] 5.6 确保共享/导入、收到文件、项目文件、加入共享、移出共享、导出保存和删除管理处于同一工作区
- [x] 5.7 保持项目历史抽屉、项目重命名、项目删除和当前项目切换功能不变

## 6. 设置页、底部 Tab 与引导层统一

- [x] 6.1 更新 `SettingsScreen`，使用同一背景、表面、列表项、图标和文本层级
- [x] 6.2 更新 `AppBottomTabBar`，让 Home/Settings 选中态、图标和安全区间距与新 token 一致
- [x] 6.3 更新 `WorkspaceOnboardingOverlay` 锚点，适配启动前只强调服务入口、启动后强调内容工作区
- [x] 6.4 确认语言设置菜单、帮助入口和底部导航不会遮挡主流程

## 7. 轻量动效与反馈

- [x] 7.1 创建或补充动效参数，包含按压反馈、面板出现、页面切换的 duration/easing/spring 配置
- [x] 7.2 为启动服务入口添加按压反馈，优先使用 `useNativeDriver: true`
- [x] 7.3 为服务启动后内容工作区出现添加轻量淡入或位移动效
- [x] 7.4 确认低端设备和 Harmony 降级时可关闭或简化动效

## 8. 功能回归验证

- [x] 8.1 验证浏览器上传文件后，当前项目文件列表正确更新
- [x] 8.2 验证浏览器提交文本后，当前项目消息列表正确更新
- [x] 8.3 验证 App 导入文件、导入媒体、加入共享、批量下载和导出保存仍可用
- [x] 8.4 验证服务启动失败、网络不可达、导入失败、导出失败和共享切换失败的反馈仍清晰可见
- [x] 8.5 验证删除项目、删除文件、删除消息等危险操作仍有明确确认或结果反馈

## 9. 跨平台验收

- [ ] 9.1 在 iOS 真机或模拟器检查首页启动前、启动中、启动后视觉层级和基础投递流程
- [ ] 9.2 在 Android 真机或模拟器检查首页启动前、启动中、启动后视觉层级和基础投递流程
- [ ] 9.3 在 Harmony 可用环境检查图标、表面、动效降级和基础投递流程
- [ ] 9.4 检查窄屏、常见手机竖屏、横屏或宽屏下文本、二维码、图标按钮和底部 Tab 不重叠

## 10. 最终检查

- [x] 10.1 运行 `npm run typecheck`
- [x] 10.2 运行 `npm run test:unit -- --runInBand`
- [x] 10.3 更新 README 或内部说明，记录 V1 不新增原生 UI 依赖和后续可选增强项
- [x] 10.4 记录未完成的真机或性能验证项，避免把未执行的验收误标为完成

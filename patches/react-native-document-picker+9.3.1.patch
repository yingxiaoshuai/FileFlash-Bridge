diff --git a/node_modules/react-native-document-picker/android/src/main/java/com/reactnativedocumentpicker/RNDocumentPickerModule.java b/node_modules/react-native-document-picker/android/src/main/java/com/reactnativedocumentpicker/RNDocumentPickerModule.java
index 5001525..1b71077 100644
--- a/node_modules/react-native-document-picker/android/src/main/java/com/reactnativedocumentpicker/RNDocumentPickerModule.java
+++ b/node_modules/react-native-document-picker/android/src/main/java/com/reactnativedocumentpicker/RNDocumentPickerModule.java
@@ -18,7 +18,6 @@ import androidx.annotation.NonNull;
 import com.facebook.react.bridge.ActivityEventListener;
 import com.facebook.react.bridge.Arguments;
 import com.facebook.react.bridge.BaseActivityEventListener;
-import com.facebook.react.bridge.GuardedResultAsyncTask;
 import com.facebook.react.bridge.Promise;
 import com.facebook.react.bridge.ReactApplicationContext;
 import com.facebook.react.bridge.ReactContext;
@@ -36,11 +35,13 @@ import java.lang.ref.WeakReference;
 import java.util.ArrayList;
 import java.util.List;
 import java.util.UUID;
+import java.util.concurrent.Executors;
 
 public class RNDocumentPickerModule extends NativeDocumentPickerSpec {
   public static final String NAME = "RNDocumentPicker";
   private static final int READ_REQUEST_CODE = 41;
   private static final int PICK_DIR_REQUEST_CODE = 42;
+  private static final int CREATE_DOCUMENT_REQUEST_CODE = 43;
 
   private static final String E_ACTIVITY_DOES_NOT_EXIST = "ACTIVITY_DOES_NOT_EXIST";
   private static final String E_FAILED_TO_SHOW_PICKER = "FAILED_TO_SHOW_PICKER";
@@ -63,6 +64,8 @@ public class RNDocumentPickerModule extends NativeDocumentPickerSpec {
 
   private Promise promise;
   private String copyTo;
+  private String pendingCreateDocumentMimeType;
+  private String pendingCreateDocumentName;
 
   public RNDocumentPickerModule(ReactApplicationContext reactContext) {
     super(reactContext);
@@ -72,7 +75,10 @@ public class RNDocumentPickerModule extends NativeDocumentPickerSpec {
   private final ActivityEventListener activityEventListener = new BaseActivityEventListener() {
     @Override
     public void onActivityResult(Activity activity, int requestCode, int resultCode, Intent data) {
-      boolean isForeignResult = requestCode != READ_REQUEST_CODE && requestCode != PICK_DIR_REQUEST_CODE;
+      boolean isForeignResult =
+          requestCode != READ_REQUEST_CODE
+              && requestCode != PICK_DIR_REQUEST_CODE
+              && requestCode != CREATE_DOCUMENT_REQUEST_CODE;
       if (isForeignResult) {
         return;
       }
@@ -82,13 +88,15 @@ public class RNDocumentPickerModule extends NativeDocumentPickerSpec {
         return;
       }
       if (resultCode == Activity.RESULT_CANCELED) {
-        sendError(E_DOCUMENT_PICKER_CANCELED, "User canceled directory picker");
+        sendError(E_DOCUMENT_PICKER_CANCELED, "User canceled the document picker");
         return;
       }
       if (requestCode == READ_REQUEST_CODE) {
         onShowActivityResult(resultCode, data, storedPromise);
-      } else {
+      } else if (requestCode == PICK_DIR_REQUEST_CODE) {
         onPickDirectoryResult(resultCode, data);
+      } else {
+        onCreateDocumentResult(resultCode, data);
       }
     }
   };
@@ -172,6 +180,33 @@ public class RNDocumentPickerModule extends NativeDocumentPickerSpec {
     }
   }
 
+  @ReactMethod
+  public void createDocument(String suggestedName, String mimeType, Promise promise) {
+    Activity currentActivity = getCurrentActivity();
+
+    if (currentActivity == null) {
+      promise.reject(E_ACTIVITY_DOES_NOT_EXIST, "Current activity does not exist");
+      return;
+    }
+
+    this.promise = promise;
+    this.pendingCreateDocumentName = suggestedName;
+    this.pendingCreateDocumentMimeType = mimeType;
+
+    try {
+      Intent intent = new Intent(Intent.ACTION_CREATE_DOCUMENT);
+      intent.addCategory(Intent.CATEGORY_OPENABLE);
+      intent.setType(
+          mimeType != null && !mimeType.isEmpty() ? mimeType : "application/octet-stream");
+      intent.putExtra(
+          Intent.EXTRA_TITLE,
+          suggestedName != null && !suggestedName.isEmpty() ? suggestedName : "fileflash-export");
+      currentActivity.startActivityForResult(intent, CREATE_DOCUMENT_REQUEST_CODE, null);
+    } catch (Exception e) {
+      sendError(E_FAILED_TO_SHOW_PICKER, "Failed to create external document", e);
+    }
+  }
+
   @Override
   public void releaseSecureAccess(ReadableArray uris, Promise promise) {
     promise.reject("RNDocumentPicker:releaseSecureAccess", "releaseSecureAccess is not supported on Android");
@@ -188,12 +223,51 @@ public class RNDocumentPickerModule extends NativeDocumentPickerSpec {
       return;
     }
     Uri uri = data.getData();
+    takePersistablePermissions(data, uri);
+
+    WritableMap map = Arguments.createMap();
+    map.putString(FIELD_URI, uri.toString());
+    promise.resolve(map);
+  }
+
+  private void onCreateDocumentResult(int resultCode, Intent data) {
+    if (resultCode != Activity.RESULT_OK) {
+      sendError(E_UNKNOWN_ACTIVITY_RESULT, "Unknown activity result: " + resultCode);
+      return;
+    }
+
+    if (data == null || data.getData() == null) {
+      sendError(E_INVALID_DATA_RETURNED, "Invalid data returned by intent");
+      return;
+    }
+
+    Uri uri = data.getData();
+    takePersistablePermissions(data, uri);
 
     WritableMap map = Arguments.createMap();
     map.putString(FIELD_URI, uri.toString());
+    map.putString(FIELD_NAME, pendingCreateDocumentName);
+    map.putString(FIELD_TYPE, pendingCreateDocumentMimeType);
+    map.putNull(FIELD_SIZE);
     promise.resolve(map);
   }
 
+  private void takePersistablePermissions(Intent data, Uri uri) {
+    if (data == null || uri == null) {
+      return;
+    }
+
+    int flags =
+        data.getFlags()
+            & (Intent.FLAG_GRANT_READ_URI_PERMISSION | Intent.FLAG_GRANT_WRITE_URI_PERMISSION);
+
+    try {
+      getReactApplicationContext().getContentResolver().takePersistableUriPermission(uri, flags);
+    } catch (SecurityException ignored) {
+      // Some providers do not support persistable permissions; best-effort is enough here.
+    }
+  }
+
   public void onShowActivityResult(int resultCode, Intent data, Promise promise) {
     if (resultCode != Activity.RESULT_OK) {
       sendError(E_UNKNOWN_ACTIVITY_RESULT, "Unknown activity result: " + resultCode);
@@ -223,38 +297,44 @@ public class RNDocumentPickerModule extends NativeDocumentPickerSpec {
         return;
       }
 
-      new ProcessDataTask(getReactApplicationContext(), uris, copyTo, promise).execute();
+      scheduleProcessPickResults(getReactApplicationContext(), uris, copyTo, promise);
     } catch (Exception e) {
       sendError(E_UNEXPECTED_EXCEPTION, e.getLocalizedMessage(), e);
     }
   }
 
-  private static class ProcessDataTask extends GuardedResultAsyncTask<ReadableArray> {
+  /**
+   * GuardedResultAsyncTask was removed from React Native; use background executor + UI queue.
+   */
+  private void scheduleProcessPickResults(
+      ReactApplicationContext reactContext,
+      List<Uri> uris,
+      String copyTo,
+      Promise promise) {
+    Executors.newSingleThreadExecutor()
+        .execute(
+            () -> {
+              try {
+                ProcessDataHelper helper = new ProcessDataHelper(reactContext, copyTo);
+                WritableArray results = Arguments.createArray();
+                for (Uri uri : uris) {
+                  results.pushMap(helper.getMetadata(uri));
+                }
+                reactContext.runOnUiQueueThread(() -> promise.resolve(results));
+              } catch (Exception e) {
+                reactContext.runOnUiQueueThread(
+                    () -> promise.reject(E_UNEXPECTED_EXCEPTION, e.getLocalizedMessage(), e));
+              }
+            });
+  }
+
+  private static class ProcessDataHelper {
     private final WeakReference<Context> weakContext;
-    private final List<Uri> uris;
     private final String copyTo;
-    private final Promise promise;
 
-    protected ProcessDataTask(ReactContext reactContext, List<Uri> uris, String copyTo, Promise promise) {
-      super(reactContext.getExceptionHandler());
+    ProcessDataHelper(ReactContext reactContext, String copyTo) {
       this.weakContext = new WeakReference<>(reactContext.getApplicationContext());
-      this.uris = uris;
       this.copyTo = copyTo;
-      this.promise = promise;
-    }
-
-    @Override
-    protected ReadableArray doInBackgroundGuarded() {
-      WritableArray results = Arguments.createArray();
-      for (Uri uri : uris) {
-        results.pushMap(getMetadata(uri));
-      }
-      return results;
-    }
-
-    @Override
-    protected void onPostExecuteGuarded(ReadableArray readableArray) {
-      promise.resolve(readableArray);
     }
 
     private WritableMap getMetadata(Uri uri) {
diff --git a/node_modules/react-native-document-picker/android/src/paper/java/com/reactnativedocumentpicker/NativeDocumentPickerSpec.java b/node_modules/react-native-document-picker/android/src/paper/java/com/reactnativedocumentpicker/NativeDocumentPickerSpec.java
index 76323fa..73ae5be 100644
--- a/node_modules/react-native-document-picker/android/src/paper/java/com/reactnativedocumentpicker/NativeDocumentPickerSpec.java
+++ b/node_modules/react-native-document-picker/android/src/paper/java/com/reactnativedocumentpicker/NativeDocumentPickerSpec.java
@@ -38,4 +38,8 @@ public abstract class NativeDocumentPickerSpec extends ReactContextBaseJavaModul
   @ReactMethod
   @DoNotStrip
   public abstract void pickDirectory(Promise promise);
+
+  @ReactMethod
+  @DoNotStrip
+  public abstract void createDocument(String name, String mimeType, Promise promise);
 }
diff --git a/node_modules/react-native-document-picker/ios/RNDocumentPicker.mm b/node_modules/react-native-document-picker/ios/RNDocumentPicker.mm
index 8cf6d55..57bfcc6 100644
--- a/node_modules/react-native-document-picker/ios/RNDocumentPicker.mm
+++ b/node_modules/react-native-document-picker/ios/RNDocumentPicker.mm
@@ -193,6 +193,13 @@ RCT_EXPORT_METHOD(releaseSecureAccess:(NSArray<NSString *> *)uris
     reject(@"RNDocumentPicker:pickDirectory", @"pickDirectory is not supported on iOS", nil);
 }
 
+- (void)createDocument:(NSString *)name
+              mimeType:(NSString *)mimeType
+               resolve:(RCTPromiseResolveBlock)resolve
+                reject:(RCTPromiseRejectBlock)reject {
+    reject(@"RNDocumentPicker:createDocument", @"createDocument is not supported on iOS", nil);
+}
+
 + (NSURL *)copyToUniqueDestinationFrom:(NSURL *)url usingDestinationPreset:(NSString *)copyToDirectory error:(NSError **)error
 {
     NSURL *destinationRootDir = [self getDirectoryForFileCopy:copyToDirectory];
diff --git a/node_modules/react-native-document-picker/src/NativeDocumentPicker.ts b/node_modules/react-native-document-picker/src/NativeDocumentPicker.ts
index 39434c8..6e2e8f1 100644
--- a/node_modules/react-native-document-picker/src/NativeDocumentPicker.ts
+++ b/node_modules/react-native-document-picker/src/NativeDocumentPicker.ts
@@ -22,6 +22,7 @@ export interface Spec extends TurboModule {
   pick(options: Object): Promise<DocumentPickerResponse[]>
   releaseSecureAccess(uris: string[]): Promise<void>
   pickDirectory(): Promise<DirectoryPickerResponse>
+  createDocument(name: string, mimeType: string): Promise<DirectoryPickerResponse>
 }
 
 export const NativeDocumentPicker = TurboModuleRegistry.getEnforcing<Spec>('RNDocumentPicker')
